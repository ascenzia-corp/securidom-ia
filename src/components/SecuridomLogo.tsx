"use client";

export default function SecuridomLogo({ className = "" }: { className?: string }) {
  return (
    <img
      src="/logo-securidom.png"
      alt="Securidom"
      className={className}
    />
  );
}
